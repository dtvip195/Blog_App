<div class="card my-4">
  <h5 class="card-header"><%= t "hashtag" %></h5>
  <div class="card-body">
    <% @hashtags.each do |h|  %>
      <%= link_to h.hashtag, hashtag_path(h), style: "color: back" %>
    <% end %>
  </div>
</div>
